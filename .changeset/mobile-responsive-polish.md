---
'@craftzbay/ui': minor
---

Mobile polish: `SelectTrigger` truncates its value instead of wrapping at 16px mobile text; `Pagination` collapses to prev/next + "page / count" below `sm` (page numbers and first/last from `sm` up); `TopNav` logo group may shrink (`min-w-0`) so a long tenant name truncates instead of pushing the actions off-screen.
