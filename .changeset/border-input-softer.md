---
'@craftzbay/ui': patch
---

Text-field borders (`--border-input`) softened to ~2.1:1 so inputs match card/table edges; new `--border-control` (≥3:1) keeps checkbox/radio edges WCAG 1.4.11-compliant where the edge is the only identifier.
